{
  "$schema": "http://json-schema.org/schema",
  "$id": "Store",
  "title": "Custom store options Schema",
  "type": "object",
  "description": "Creates a new store definition in the given or default project.",
  "properties": {
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path at which to create the store file, relative to the current workspace. Default is a folder with the same name as the component in the project root.",
      "visible": false
    },
    "name": {
      "type": "string",
      "description": "The name of the entity.",
      "x-prompt": "What name would you like to use for the entity?"
    }
  },
  "required": [
    "name"
  ]
}